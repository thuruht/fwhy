{
  "name": "fwhy-dev-front",
  "version": "1.0.0",
  "description": "Farewell Cafe website deployed as Cloudflare Worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "preview": "wrangler dev --local"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "static-site",
    "farewell-cafe"
  ],
  "author": "Farewell Cafe",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.84.1"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  }
}
